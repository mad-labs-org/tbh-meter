# The READER/METER (Windows, where the game lives) is now ZERO-DEP: pure ctypes + stdlib.
# Nothing to install to collect — just `python` on Windows.
# (pymem was REMOVED; replaced by tbh_meter/memory/* in ctypes, validated.)
#
# Optional, only for the LEGACY live panel (main.py v1; the product doesn't need it):
#   rich>=13.7.0
#
# Mac/server side (analysis/disasm — outside the reader) uses whatever you like
# with no restriction (e.g. capstone for disassembly).
